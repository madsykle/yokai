<?xml version="1.0" encoding="utf-8"?>
<!-- iOS 27 Liquid Glass transparency slider (DESIGN.md §1.5, §2.2).

     Laid out as title + current stop + slider rather than the usual title/current-value
     row, because the point of this preference is the drag: the glass chrome re-tints
     under the finger, so the slider has to be visible without opening a dialog. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="?android:attr/listPreferredItemPaddingStart"
    android:paddingEnd="?android:attr/listPreferredItemPaddingEnd"
    android:paddingTop="@dimen/space_3"
    android:paddingBottom="@dimen/space_2">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/glass_transparency_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceListItem" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/glass_transparency_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceListItemSecondary"
        android:textColor="?android:attr/textColorSecondary" />

    <SeekBar
        android:id="@+id/glass_transparency_seekbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_1"
        android:paddingStart="0dp"
        android:paddingEnd="0dp" />

</LinearLayout>
